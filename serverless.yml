service: serverless

provider:
  name: aws
  runtime: nodejs12.x
  region: us-west-1
  stage: dev
  lambdaHashingVersion: 20201221

plugins:
  - serverless-offline

functions:
  helloworld:
    handler: src/helloworld.handler
    
